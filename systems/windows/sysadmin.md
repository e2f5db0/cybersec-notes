# Windows sysadmin tools

Microsoft Management Console (`mmc`) is the root of configurable Microsoft Service Console (.msc) panels.

## Useful programs & snap-ins

- `msconfig` – System configuration
- `compmgmt.msc`– Computer management
- `wf.msc` - Windows firewall
- `msinfo32` – System info
- `resmon` – Resource monitor